import React, { Component } from 'react';

import { DesktopContainer } from './navbar/Navbar'

export default class Sobre extends Component {
    render() {
        return (
            <div>
                <DesktopContainer />
            </div>
        );
    }
}
